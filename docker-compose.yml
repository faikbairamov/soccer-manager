services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-soccer}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-soccer}
      POSTGRES_DB: ${DB_NAME:-soccer_manager}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-soccer} -d ${DB_NAME:-soccer_manager}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
